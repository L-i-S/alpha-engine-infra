apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: alpha-engine-db

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: local-path

  resources:
    requests:
      storage: 2Gi